#include "main.h"

/**
 * _isdigit -  a function that checks for a digit 0-9
 * @d: input
 * Return: 1 if d is a digit 0 otherwise
 */
int _isdigit(int c)
{
	if ((d >= 48) && (d <= 57))
		return (1);
	else
		return (0);
}

